@model Oakinstream.Models.BlogImage

@{
    ViewBag.Title = "Upload";
}

<div class="container blogimages-container">
    <div class="row blogimages-header">
        <div class="col-md-12">
            <h1 class="text-uppercase"><span class="blogimages-title">@ViewBag.Title</span></h1>
        </div>
        <div class="btn">
            <a class="btn blogimages-btn-back" href="@Url.Action("Index", "BlogImages")"><span class="glyphicon glyphicon-backward"></span> Back to List</a>
        </div>
    </div>

    @using (Html.BeginForm("Upload", "BlogImages", FormMethod.Post, new { enctype = "multipart/form-data" }))
    {
        @Html.AntiForgeryToken()

        <div class="jumbotron site-border-normal blogcategory-jumbotron">
            @Html.ValidationSummary(true, "", new { @class = "text-danger" })

            <div class="row">
                <div class="col-md-12">
                    <div class="blogcategory-jumbotron-text">
                        <h3 class="blogimages-text"><span class="glyphicon glyphicon-picture"></span>  @Html.LabelFor(model => model.FileName):</h3>
                    </div>
                    <div class="blogcategory-jumbotron-input">
                        <input type="file" name="files" id="files" multiple="multiple" class="site-form-file btn-default" accept=".png,.jpg,.jpeg,.gif" />
                        @Html.ValidationMessageFor(model => model.FileName, "", new { @class = "text-danger" })
                    </div>
                    <div class="blogcategory-jumbotron-submit">
                        <input type="submit" value="Add >>" class="btn btn-default site-form-height" />
                    </div>
                </div>
            </div>
        </div>
    }
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
